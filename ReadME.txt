[1] Install the following modules
    AZ
    AZTable
[2] Updates the values in Config file
 - Resource group - Where stroage account is created and Azure Table is created
[3] To run the script ->>> Create_CSV_Upload.ps1 -ConfigFile Config.json